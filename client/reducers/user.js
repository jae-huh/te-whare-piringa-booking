import Auth from '../auth'

const initialState = {
  fullName: null,
  email: null,
  phoneNumber: null
}

const auth0 = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default auth0
